<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="#">Des Moines Code</a>
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <div class="nav-collapse navbar-responsive-collapse collapse">
        <ul class="nav">
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">User Groups<b class="caret"></b></a>
            <ul class="groups dropdown-menu">
              {{#each group in controller}}
                <li>{{#link-to 'group' group.id classNames="group-link"}}{{ group.name }}{{/link-to}}</li>
              {{/each}}
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>

{{ outlet }}
